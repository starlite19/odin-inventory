<!DOCTYPE html>
<html lang="en">

<head>
  <title><%= title %></title>
</head>

<body>
  <h1><%= heading %></h1>
  <div class="list">
    <% backpacks.forEach( backpack => { %>
    <%- include('backpack', {url: url, backpack: backpack}); %>
    <% }); %>
  </div>
</body>

</html>